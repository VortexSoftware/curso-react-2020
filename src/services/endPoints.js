const PATH = `http://dev.expenses.vortexsoftware.com.ar/api/v1`

const FETCH_GASTOS = `${PATH}/transactions/myExpenses`
const LOGIN = `${PATH}/users/login`

export default {
    FETCH_GASTOS,
    LOGIN
}